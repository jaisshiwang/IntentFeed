{
  "name": "intentfeed",
  "version": "0.1.0",
  "private": true,
  "description": "A Chrome extension that redirects doomscrolling into goal-aligned content.",
  "scripts": { "test": "node --test", "check": "node scripts/validate-extension.js" },
  "engines": { "node": ">=18" },
  "license": "MIT"
}
